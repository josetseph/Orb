# Core FastAPI & Web Framework
fastapi==0.128.0
uvicorn[standard]==0.40.0
python-multipart==0.0.21
pydantic==2.12.5
pydantic-settings==2.12.0

# Database & Storage
sqlalchemy==2.0.45
asyncpg==0.31.0
aiosqlite==0.20.0
greenlet>=3.0.0
watchdog==6.0.0
kuzu==0.11.3
qdrant-client==1.17.1
# typesense==2.0.0  # replaced by Meilisearch
meilisearch==0.34.1
boto3==1.40.61
aioboto3==15.5.0
aiobotocore==2.25.1
botocore==1.40.61

# LLM & AI Libraries
openai==2.15.0
anthropic==0.76.0
google-genai==1.59.0
instructor==1.14.3
# In-process GGUF inference (no llama-server). Prefer a GPU build:
#   macOS Metal:  CMAKE_ARGS="-DGGML_METAL=on" pip install llama-cpp-python --force-reinstall --no-cache-dir
#   CUDA:         CMAKE_ARGS="-DGGML_CUDA=on"  pip install llama-cpp-python --force-reinstall --no-cache-dir
#   CPU only:     pip install llama-cpp-python
llama-cpp-python>=0.3.0
# Qwen3-ASR/Marlin (in-process): see requirements-multimodal.txt
# (installed on demand by Setup / supervisor — keeps base image smaller)

# LangChain & Agents
langgraph==1.0.6
langchain-openai==1.1.7

# Lightweight media probing (video stream detection only)
av

# Document parsing (non-ML)
python-docx==1.1.2
openpyxl==3.1.5
PyMuPDF==1.26.7
Pillow==12.3.0

# Utilities
python-dateutil==2.9.0.post0
dateparser==1.2.2
tenacity==9.1.2
httpx==0.28.1
requests==2.32.5
python-dotenv==1.2.1
huggingface_hub>=0.34.0

# Data Processing
numpy==2.2.6
scikit-learn==1.8.0
regex==2026.1.15
json-repair==0.55.0
