{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "shell",
  "description": "The bundled first-run page: full access to the shell's own commands and native dialogs.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default", "opener:default"]
}
